{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "setup": "npm install && composer install && php artisan key:generate && php artisan config:cache",
        "serve": "php artisan serve",
        "serve-dev": "concurrently \"npm run dev\" \"php artisan serve\"",
        "fresh": "php artisan migrate:fresh --seed",
        "optimize": "php artisan config:cache && php artisan route:cache && php artisan view:cache",
        "clear": "php artisan config:clear && php artisan cache:clear && php artisan route:clear && php artisan view:clear"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
